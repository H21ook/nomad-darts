# Implementation Plan — Post-Implementation Wrap-up (version bump, artifacts commit, tests)

- **Workflow ID:** WF-20260807-102829-wf-20260807-post-impl-tasks
- **Date:** 2026-08-07
- **Status:** Validated
- **Scope:** Three wrap-up items from the previous workflow's final notes: (1) version bump per AGENT.md rule 9, (2) commit untracked workflow artifacts, (3) add automated tests for matchSlice + lib/utils (R4 follow-up).

## 1. Objective

Close out the WF-20260807-043915-research-plan implementation with: a version bump commit (rule 9), a commit of the durable docs + workflow scaffolding artifacts, and a Vitest unit-test suite covering the game logic (matchSlice) and pure helpers (lib/utils).

## 2. Context (research summary)

Research reports in `.opencode/workflows/WF-20260807-102829-wf-20260807-post-impl-tasks/research/`:
- `01-matchslice-testability.md` — matchSlice is pure (6 sync reducers, no thunks/DOM); helpers in redux/utils.ts pure; lib/utils.ts fully testable. Blockers: `RootState` value import from ./store (elided by esbuild — no mocking needed), nanoid v5 ESM-only + `@/` alias (needs vitest alias), Math.random/Date.now (use randomOrder:false + invariant asserts). Discrepancy: submitTurn defaults dartsUsed=0 vs ADR-0002 wording "defaults to 3" — UI always passes 1/2/3, so behavior is correct; tests assert actual behavior.
- `02-vitest-setup.md` — Vitest 4.1.x + vite-tsconfig-paths; node env (no jsdom); config with tsconfigPaths plugin; `"test": "vitest run"` script; pnpm 10 may block esbuild postinstall (mitigation: pnpm approve-builds).

## 3. Tasks

| Task | Title | Depends | Owned files | Priority |
|---|---|---|---|---|
| T01 | Version bump 0.0.02 → 0.0.03 (rule 9) | — | `package.json` | HIGH |
| T02 | Commit workflow artifacts (.gitignore block, .opencode/.gitkeep, docs/) | — | `.gitignore`, `.opencode/archive/.gitkeep`, `.opencode/workflows/.gitkeep`, `docs/decisions/`, `docs/implementations/`, `docs/plans/`, `docs/system/`, `docs/tasks/` | MEDIUM |
| T03 | Vitest setup + matchSlice/utils tests (R4) | T01 (package.json conflict — sequential) | `package.json`, `vitest.config.ts` (new), `src/lib/__tests__/utils.test.ts` (new), `src/lib/redux/__tests__/matchSlice.test.ts` (new); flagged: `src/lib/redux/matchSlice.ts` (import type hardening, optional) | HIGH |

## 4. Verification strategy

- T01: `git show HEAD --stat` (only package.json), version string check.
- T02: `git status --short` clean for the owned paths; `git show --stat` shows only intended files.
- T03: `pnpm test` (all pass), `npx tsc --noEmit`, `pnpm lint`, `pnpm build` (regression).
- Integration: full tsc + lint + build + test after all tasks.

## 5. Risks & notes

- T01/T03 both touch package.json → run sequentially (T01 first).
- pnpm 10 may block esbuild postinstall → `pnpm approve-builds` or `onlyBuiltDependencies` in package.json.
- Tests must use `randomOrder: false` and assert invariants (nanoid/Date.now non-determinism).
- ADR-0002 wording nit (dartsUsed default) — documented, no code change; tests assert actual behavior.
- Version history: 0.2.0 → 0.0.02 (45bbcdf). Next semver: 0.0.03.
